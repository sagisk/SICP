Write a program that opens all .txt files in a folder and searches for any
line that matches a user-supplied regular expression. The results should
be printed to the screen. 

import os, re
print('Provide path to the files (use only one \), please')
path = input()
print('What are you searchin\' for?')
item = input()
content = [None] * len(os.listdir(path))
for i in range(0,len(os.listdir(path))):
  if os.listdir(path)[i].endswith('txt'):
       filename = os.listdir(path)[i]
       content[i] = open(path + "\\" + filename).read()
       if content[i] != None:
        print(re.compile("".join(item)).search(content[i]).group())
  else:
        continue
