return 0 if f has never been called (rendering this call meaningless in the addition expression), and the argument given in the previous call otherwise
reference :http://community.schemewiki.org/?sicp-ex-3.8 Anon

(define f
  (let ((called #f))
   (lambda (x)
    (if called 
          0
        (set! called #t)
        x)))))
