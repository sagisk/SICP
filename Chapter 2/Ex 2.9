(define (div-interval x y)
 (mul-interval x
 (make-interval (/ 1.0 (upper-bound y))
 (/ 1.0 (lower-bound y)))))
 
;;modified
(define (div-interval x y)
  (if (or (and (> (upper-bound x) 0 (< (lower-bound x) 0))
          (and (> (upper-bound y) 0 (< (lower-bound y) 0)))
          (error "Spin 0")
          (mul-interval x
 (make-interval (/ 1.0 (upper-bound y))
 (/ 1.0 (lower-bound y)))))
          
  
